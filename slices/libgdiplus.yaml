# interface library for System.Drawing of Mono
package: libgdiplus

essential:
  - libgdiplus_copyright

slices:
  libs:
    essential:
      - libc6_libs
      - libcairo2_libs
      # - libexif12_libs # needs adding
      # - libfontconfig1_libs # needs adding
      - libgif7_libs
      # - libglib2.0-0t64_libs # needs adding
      # - libharfbuzz0b_libs # needs adding
      # - libjpeg-turebo8_libs # needs adding
      # - libpango-1.0-0_libs # needs adding
      - libpng16-16t64_libs
      - libtiff6_libs
    contents:
      /usr/lib/libgdiplus.so*:
      /usr/lib/pkgconfig/libgdiplus.pc:

  copyright:
    contents:
      /usr/share/doc/libgdiplus/copyright:
